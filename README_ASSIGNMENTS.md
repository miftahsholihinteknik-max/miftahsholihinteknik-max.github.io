# Final Assignment Module

Upload/replace these files in your GitHub Pages repository:

- `index.html`
- `js/app.js`
- `css/assignments.css`
- `data/assignments.json`

Keep your existing `css/style.css`, `data/profile.json`, `data/publications.json`, `data/courses.json`, `data/resources.json`, `images/`, and `cv/`.

The Teaching section now has:
- Explore resources
- Explore assignments

Assignment data is managed entirely from `data/assignments.json`.

Replace placeholder Google Drive, YouTube, Colab, and Google Forms URLs with your real links.
